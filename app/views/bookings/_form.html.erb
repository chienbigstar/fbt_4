<%= form_for @book, remote: true do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :tour_id, value: @tour.id %>
  <%= f.label @tour.name %>
  <%= f.number_field :numbermember %>
  <%= f.submit :OK %>
<% end %>
